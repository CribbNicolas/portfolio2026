node_modules/
# Credenciales locales (Cloudflare, Clarity). NUNCA se commitean: el token de
# Pages tiene permiso de escritura sobre los deploys de la cuenta.
.env
.env.*
!.env.example
.dev.vars
dist/
.astro/
# `wrangler pages dev dist`, used to load the built site under the real
# `public/_headers` before shipping them. Local state, never an artifact.
.wrangler/
# Generated by `pdf:local` so `astro dev` can serve /cv.pdf and /en/cv.pdf.
# Production is the Function.
public/cv.pdf
public/en/cv.pdf
# Scratch de ejecución de planes (ledger, briefs, reportes). El registro real es git.
.superpowers/
*.log
.DS_Store
_smoke.ts
# editor/store.ts's tmp-then-rename write, orphaned if the process dies
# between the two: it lands next to the tracked dataset it is standing in for.
content/data/*.tmp-*
